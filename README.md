# 🌙 مسابقة رمضان العائلية

## ملفات المشروع

```
ramadan-quiz/
├── backend/
│   ├── main.py          ← الـ Backend (FastAPI)
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── index.html       ← صفحة المشاركين
│   └── admin.html       ← لوحة تحكم المشرف
├── docker-compose.yml
└── README.md
```

---

## 🚀 طريقة التشغيل

### الخيار ١: تشغيل محلي (للاختبار)

```bash
# تثبيت المتطلبات
cd backend
pip install -r requirements.txt

# تشغيل الخادم
uvicorn main:app --reload --port 8000
```

ثم افتح:

- **صفحة المشاركة:** `frontend/index.html`
- **لوحة الإدارة:** `frontend/admin.html`

> ⚠️ قبل النشر للعموم، عدّل سطر `const API = '/api'` في كلا الملفين ليشير لرابط السيرفر الفعلي

---

### الخيار ٢: نشر مجاني على Render

1. ارفع المشروع على GitHub
2. سجّل في [render.com](https://render.com)
3. أنشئ **Web Service** واختر المستودع
4. Root Directory: `backend`
5. Start Command: `uvicorn main:app --host 0.0.0.0 --port 8000`
6. أضف متغير البيئة: `ADMIN_SECRET` = كلمة مرور تختارها
7. بعد النشر، عدّل `const API` في ملفات HTML برابط Render

للـ Frontend، يمكن رفع الملفين على [GitHub Pages](https://pages.github.com) مجاناً.

---

### الخيار ٣: Docker

```bash
# تغيير كلمة المرور في docker-compose.yml أولاً!
docker-compose up -d
```

---

## 🔑 كلمة مرور المشرف

الافتراضية: **`ramadan2025admin`**

> غيّرها في متغير `ADMIN_SECRET` قبل النشر!

---

## 📋 تعليمات الاستخدام

### كمشرف:

1. افتح `admin.html` وادخل بكلمة المرور
2. من **الإعدادات**: حدد اليوم الحالي ووقت فتح المسابقة (مثلاً 21:00)
3. من **إدارة الأسئلة**: أضف ٣ أسئلة لكل يوم
4. أرسل رابط `index.html` للعائلة كل ليلة بعد التراويح
5. في **المتصدرون**: تابع النقاط لحظة بلحظة
6. في يوم العيد: صدّر البيانات واعلن النتائج 🎉

### للمشارك:

1. يفتح الرابط
2. ينتظر عداد الوقت حتى الـ ٩ مساءً
3. يسجّل اسمه (ورقم جواله اختياري)
4. يجاوب على ٣ أسئلة - ٣٠ ثانية لكل سؤال
5. يرى نتيجته ويواصل كل يوم لتجميع النقاط

---

## ⭐ ميزات النظام

- ✅ عداد تنازلي للوقت المتبقي لفتح المسابقة
- ✅ تسجيل بالاسم + الجوال (الجوال اختياري ولكن لا يتكرر)
- ✅ عداد ٣٠ ثانية لكل سؤال
- ✅ تصنيفات: قرآن كريم / رمضان / عام
- ✅ لوحة تحكم كاملة للمشرف
- ✅ نظام نقاط تراكمي
- ✅ تصدير البيانات CSV
- ✅ تصميم عربي رمضاني جميل
- ✅ يعمل على الجوال بدون تحميل

---

رمضان كريم 🌙
