version: "3.8"
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - ADMIN_SECRET=${ADMIN_SECRET:-ramadan2026admin}
    restart: unless-stopped
